<h1 class="text-4xl font-extrabold text-center my-6"><%= @restro.name %></h1>

<div class="px-8 py-12 bg-gray-100">
  <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
    <% @products.each do |product| %>
      <div class="bg-white shadow-lg rounded-lg p-6 transition-all duration-300 transform hover:scale-105 hover:shadow-2xl">
        <h2 class="text-2xl font-semibold text-gray-800 mb-2"><%= product.name %></h2>

        <p class="text-lg text-gray-600 mb-4">
          <%= number_to_currency(product.price, unit: "₹", separator: ".", delimiter: ",", format: "%u %n") %>
        </p>

        <%= button_to 'Add to Cart', '#', class: 'w-full py-2 px-4 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors' %>
      </div>
    <% end %>
  </div>
</div>
